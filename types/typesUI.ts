export interface MatterStampIconProps {
  size?: number;
  color?: string;
  stroke?: number;
}

export interface TypographyProps {
  variant: string;
  // eslint-disable-next-line react/require-default-props
  color?: string;
  // eslint-disable-next-line react/require-default-props
  customedStyles?: Object;
  children: string;
}
